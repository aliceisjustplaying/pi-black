From e602e3162b2a06ef60060e6abda8e28155ea20ab Mon Sep 17 00:00:00 2001
From: Pi OAuth Compatibility Maintainers <noreply@example.invalid>
Date: Sat, 19 Sep 2026 00:35:00 +0000
Subject: [PATCH] feat(ai): add Claude Code model context

---
 packages/ai/src/api/anthropic-claude-code.ts   | 13 +++++++++++++
 packages/ai/src/api/anthropic-messages.ts      |  3 +++
 packages/ai/test/anthropic-claude-code.test.ts | 14 ++++++++++++++
 3 files changed, 30 insertions(+)

diff --git a/packages/ai/src/api/anthropic-claude-code.ts b/packages/ai/src/api/anthropic-claude-code.ts
index 753a3017..515a1644 100644
--- a/packages/ai/src/api/anthropic-claude-code.ts
+++ b/packages/ai/src/api/anthropic-claude-code.ts
@@ -15,6 +15,19 @@ const PRIME64_3 = 0x165667b19e3779f9n;
 const PRIME64_4 = 0x85ebca77c2b2ae63n;
 const PRIME64_5 = 0x27d4eb2f165667c5n;
 
+const CLAUDE_CODE_MODEL_CONTEXTS: Record<string, string> = {
+	"claude-fable-5-1":
+		"You are powered by the model named Fable 5.1. The exact model ID is claude-fable-5-1. Assistant knowledge cutoff is June 2026.",
+	"claude-opus-5":
+		"You are powered by the model named Opus 5. The exact model ID is claude-opus-5. Assistant knowledge cutoff is May 2026.",
+	"claude-sonnet-5":
+		"You are powered by the model named Sonnet 5. The exact model ID is claude-sonnet-5. Assistant knowledge cutoff is January 2026.",
+};
+
+export function claudeCodeModelContext(modelId: string): string | undefined {
+	return CLAUDE_CODE_MODEL_CONTEXTS[modelId];
+}
+
 function isObject(value: unknown): value is Record<string, unknown> {
 	return typeof value === "object" && value !== null && !Array.isArray(value);
 }
diff --git a/packages/ai/src/api/anthropic-messages.ts b/packages/ai/src/api/anthropic-messages.ts
index 0ef3701d..fc0b8854 100644
--- a/packages/ai/src/api/anthropic-messages.ts
+++ b/packages/ai/src/api/anthropic-messages.ts
@@ -40,6 +40,7 @@ import { sanitizeSurrogates } from "../utils/sanitize-unicode.ts";
 import {
 	buildClaudeCodeBillingHeader,
 	CLAUDE_CODE_VERSION,
+	claudeCodeModelContext,
 	createClaudeCodeFetch,
 	latchClaudeCodeAtis,
 } from "./anthropic-claude-code.ts";
@@ -980,12 +981,14 @@ function buildParams(
 
 	// For OAuth tokens, we MUST include Claude Code identity
 	if (isOAuthToken) {
+		const modelContext = claudeCodeModelContext(model.id);
 		params.system = [
 			...(billingHeader ? [{ type: "text" as const, text: billingHeader }] : []),
 			{
 				type: "text",
 				text: "You are a Claude agent, built on Anthropic's Claude Agent SDK.",
 			},
+			...(modelContext ? [{ type: "text" as const, text: modelContext }] : []),
 		];
 		if (context.systemPrompt) {
 			params.system.push({
diff --git a/packages/ai/test/anthropic-claude-code.test.ts b/packages/ai/test/anthropic-claude-code.test.ts
index d59b7ebd..ab918be8 100644
--- a/packages/ai/test/anthropic-claude-code.test.ts
+++ b/packages/ai/test/anthropic-claude-code.test.ts
@@ -1,6 +1,7 @@
 import { describe, expect, it, vi } from "vitest";
 import {
 	buildClaudeCodeBillingHeader,
+	claudeCodeModelContext,
 	claudeCodeVersionFingerprint,
 	createClaudeCodeFetch,
 	parseClaudeCodeAtis,
@@ -25,6 +26,19 @@ describe("Claude Code Anthropic request compatibility", () => {
 		);
 	});
 
+	it("provides verified Claude Code model identity and cutoff context", () => {
+		expect(claudeCodeModelContext("claude-fable-5-1")).toBe(
+			"You are powered by the model named Fable 5.1. The exact model ID is claude-fable-5-1. Assistant knowledge cutoff is June 2026.",
+		);
+		expect(claudeCodeModelContext("claude-opus-5")).toBe(
+			"You are powered by the model named Opus 5. The exact model ID is claude-opus-5. Assistant knowledge cutoff is May 2026.",
+		);
+		expect(claudeCodeModelContext("claude-sonnet-5")).toBe(
+			"You are powered by the model named Sonnet 5. The exact model ID is claude-sonnet-5. Assistant knowledge cutoff is January 2026.",
+		);
+		expect(claudeCodeModelContext("claude-haiku-5")).toBeUndefined();
+	});
+
 	it("selects the newest model-specific ATIS assignment", () => {
 		const state = {
 			clientDataCacheSlots: {
-- 
2.54.0 (Apple Git-157)

